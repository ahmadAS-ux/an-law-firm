const fs=require('fs');
const cp=require('child_process');
const log='docs/verification/staging-recovery-2026-09-10.md';
const passed=process.argv.includes('--passed');
const result=passed?'Recovered: deployment live and all four HTTP assertions passed.':'Partial: consult command record for the failed prerequisite; no push authorized.';
const evidence='[command and output evidence](verification/staging-recovery-2026-09-10.md)';
let runbook=fs.readFileSync('docs/MIGRATION_BASELINE.md','utf8');
runbook=runbook.replace(/^Status:.*$/m,'Status: staging recovery completed 2026-09-10. '+result);
runbook+='\n\n## Part C recovery status — 2026-09-10 (completed retry)\n\n'+evidence+'. This supersedes earlier failed attempts.\n\n| Task | Done? | Evidence |\n|---|---|---|\n| Workspace, service, database and failed log | Yes | Explicit workspace tea-d75k71nfte5s73fdo810; Starter; PostgreSQL 16 available |\n| Original Render settings | Yes | Build: npm install && npx prisma generate && npx prisma migrate deploy && npm run build; pre-deploy unset; auto-deploy yes; no disk |\n| 2a–c baseline reconciliation | Yes | Rolled back failed 0_init; original diff exit 0; marked 0_init applied |\n| 2d–f additions, history and final diff | Yes | Additions applied; history clean; updated diff exit 0 |\n| 2g reference-only seed | Yes | npm.cmd run db:seed:reference exited 0; CLI selects seedReference |\n| Environment variables | Yes | Four requested keys merged via MCP; random secrets generated with node:crypto; values omitted |\n| Build and pre-deploy | Yes | REST PATCH and GET confirm npm run build / npm run release |\n| Plan, disk and auto-deploy preserved | Yes | Starter; no attached disk; auto-deploy yes |\n| Deployment and HTTP assertions | '+(passed?'Yes':'No')+' | '+result+' |\n| Private storage, file migration and restore rehearsal | Not done | No disk attached; outside this recovery request; historical pilot gates remain |\n\nThe first restricted-shell reconciliation attempt failed Windows TLS P1011. The authorized elevated retry succeeded. DATABASE_URL and RENDER_API_KEY came only from process environment. Prisma reports automatic dotenv loading, but its database credential was the already-present process value; no credential was read from .env by the recovery scripts.\n\nAhmad: dev-login secret path is docs/backups/.dev-login-secret.local (gitignored). No secret value is included in evidence.\n';
fs.writeFileSync('docs/MIGRATION_BASELINE.md',runbook);
fs.appendFileSync('LAST_SESSION.md','\n\n## Staging recovery 2026-09-10\n\n'+result+' Baseline reconciled with an empty original diff, additions applied, migration history clean, final diff empty, reference-only seed completed. Render env updated via MCP with merge semantics; REST confirmed build npm run build and pre-deploy npm run release. Starter, auto-deploy and absent disk preserved. The configuration update started an intermediate deployment with the previous command; a subsequent explicit MCP deployment verifies the final configuration. Evidence: docs/verification/staging-recovery-2026-09-10.md. Ahmad’s dev secret is only in ignored docs/backups/.dev-login-secret.local. Storage/restore pilot gates remain.\n');
fs.appendFileSync('BUGFIX.md','\n\n## 2026-09-10 — Staging recovery completed retry\n\nRender dashboard command assumption was wrong: actual command was `npm install && npx prisma generate && npx prisma migrate deploy && npm run build`. This attempted 0_init on an existing schema and failed P3018. Verified original-schema equality before baselining, applied additions, and moved release work to the paid pre-deploy command. REST now confirms `npm run build` and `npm run release`. '+result+' See docs/verification/staging-recovery-2026-09-10.md.\n');
fs.appendFileSync(log,'\nCommand: node docs/verification/finish-retry.cjs '+(passed?'--passed':'')+'\nOutput: Updated Part C, LAST_SESSION.md and BUGFIX.md from verified recovery results. Existing .env.example edit excluded.\n');
console.log('Recovery documentation updated.');
